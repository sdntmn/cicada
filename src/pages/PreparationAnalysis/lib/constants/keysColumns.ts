export enum VirtualColumnAnalysisTableKey {
  ACTION = "action",
  DEBT = "debt",
  DEBTOR = "debtor",
  DOCUMENTS = "documents",
  STAGE = "stage",
}

export enum BaseColumn {
  INDEX = "rowIndex",
}
