export enum FilterMode {
  ALL = "all",
  ANY = "any",
}

export enum BaseColumnTableSelect {
  ACCOUNT = "account",
  ADDRESS = "address",
  CITY = "city",
  DEBT = "debt",
  FIO = "fio",
  PENALTY = "penalty",
}

export enum VirtualColumnTableSelect {
  INDEX = "index",
}
