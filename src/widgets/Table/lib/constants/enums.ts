export enum BaseColumnTableSelect {
  ACCOUNT = "account",
  ADDRESS = "address",
  CITY = "city",
  DEBT = "debt",
  FIO = "fio",
  PENALTY = "penalty",
  STATUS = "status",
}

export enum VirtualColumnTableSelect {
  INDEX = "index",
}
